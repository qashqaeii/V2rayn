تو یک Senior Android + Networking Engineer (Production Level) هستی که وظیفه‌ات تبدیل یک پروژه Open-Source به یک Commercial White-Label VPN Client است.

این کار یک Hard Commercial Fork محسوب می‌شود، نه توسعه عادی.

🔽 مرحله 0 — کلون سورس اصلی

ریپازیتوری رسمی:

https://github.com/2dust/v2rayNG.git


❗ پروژه جدید ساخته نمی‌شود
❗ معماری اصلی حفظ می‌شود
❗ فقط Refactor / Modify / Remove
❗ این پروژه در نهایت یک محصول تجاری Production خواهد بود

🎯 هدف نهایی

تبدیل کلاینت عمومی V2Ray به یک اپلیکیشن VPN اختصاصی که:

کاربر نتواند هیچ کانفیگی وارد کند

هیچ Subscription نداشته باشد

هیچ Import نداشته باشد

هیچ QR Scan نداشته باشد

هیچ سروری را نتواند ویرایش یا حذف کند

فقط سرورهای ما از API لود شوند

کاملاً ریبرند شود

یک Commercial Locked VPN Client باشد

این اپ دیگر «V2Ray Client» نیست.

🔎 مرحله 1 — شناسایی سیستم مدیریت سرورها

در کل سورس جستجو شود برای:

add server
import
subscription
qr
scan
clipboard
edit server
remove server
share config
manual config
vmess link
vless link


لیست شود:

Activity ها

Fragment ها

Dialog ها

ViewModel ها

Repository ها

Database DAO ها

مرتبط با مدیریت سرور.

❌ مرحله 2 — حذف کامل قابلیت‌های کلاینت عمومی

این قابلیت‌ها باید از UI + Logic + Database حذف شوند:

افزودن سرور دستی

Import از لینک

Import از فایل

Subscription URL

QR Code Scan

ویرایش سرور

حذف سرور

Export / Share Config

Clipboard Import

کاربر نباید هیچ راهی برای ساخت یا تغییر سرور داشته باشد.

🌐 مرحله 3 — منبع سرورها = فقط API

هرجا سرورها از:

دیتابیس

فایل

Subscription

لود می‌شوند، تغییر داده شود تا فقط از این API بیایند:

GET https://api.example.com/vpn/servers


نمونه پاسخ:

[
  {
    "id": "srv1",
    "name": "Germany #1",
    "flag": "de",
    "config": "vless://uuid@host:port?params#Germany"
  }
]

🏳 فیلد جدید:

flag → برای نمایش پرچم کشور در UI

🧩 مرحله 4 — تبدیل لینک VLESS

از parserهای داخلی v2rayNG استفاده شود:

VMess parser

VLESS parser

مقدار config از API → تبدیل به ساختار داخلی V2Ray
و سپس به لیست سرورها Inject شود.

هیچ سیستم جدیدی برای parsing نوشته نشود.

🔒 مرحله 5 — قفل کردن کامل سیستم

دیتابیس سرورها Read-Only شود

هر بار اجرای اپ:

لیست سرورها از API ریفرش شود

سرورهای غیر API حذف شوند

هیچ دکمه‌ای برای Add/Edit/Delete وجود نداشته باشد

اگر API در دسترس نبود → سرورها خالی شوند (امنیت بالاتر)

🎨 مرحله 6 — ریبرند کامل

تغییر داده شود:

App Name

Package Name

App Icon

Splash Screen

About Screen

Notification Title

Settings Title

هر reference به:

v2ray
v2rayNG
vmess client


از UI حذف یا تغییر داده شود.

📱 مرحله 7 — ساده‌سازی UI

صفحه اصلی فقط شامل:

وضعیت اتصال

لیست سرورهای API (با نام + پرچم)

دکمه Connect / Disconnect

حذف شود:

تنظیمات پیشرفته

Routing

DNS

Custom Config

Manual Mode

⚙️ مرحله 8 — منطق اتصال

VPN Service و Core منطق اتصال دست نخورده بماند.
فقط منبع سرورها تغییر می‌کند.

🚀 مرحله 9 — CI/CD بدون Android Studio

پروژه باید طوری تنظیم شود که:

از طریق GitHub Actions یا GitLab CI

با Gradle CLI

بدون نیاز به Android Studio

فایل APK Production بیلد شود.

لازم است:

workflow YAML

keystore signing از طریق secrets

buildTypes = release (minify + shrink)

خروجی: Signed Production APK

🌍 مرحله 10 — Production Environment

این پروژه برای محیط واقعی است:

API Production

مدیریت خطا

Timeout Handling

Retry منطقی

Crash-free رفتار شبکه

اطلاعات سرور باید از طریق Environment Variables / Secure Config استفاده شود، نه هاردکد در سورس.

📦 خروجی مورد انتظار

در پایان باید ارائه شود:

لیست فایل‌های تغییر کرده

فایل‌های حذف شده

کلاس‌های جدید:

API Client

Data Model (Server)

Repository جدید

دیاگرام جریان داده:

API → Parser → Internal Server Model → DB (ReadOnly) → UI → VPN Service

✅ نتیجه نهایی

یک:

🔐 Commercial White-Label Locked VPN Client

نه یک کلاینت عمومی V2Ray.