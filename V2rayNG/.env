# API بک‌اند جنگو (پوشه backend) — آدرس پایه باید با / تمام شود
VPN_API_BASE_URL=http://77.110.116.139:8000/api/
VPN_API_HOST=77.110.116.139

# کلید رمزگشایی کانفیگ — باید دقیقاً با CONFIG_ENCRYPTION_KEY در backend/.env یکسان باشد (۳۲ کاراکتر)
CONFIG_ENCRYPTION_KEY=vpnclient-aes256-key-32bytes!!!!

# اختیاری: برای HTTPS و Certificate Pinning (خالی بگذارید اگر از HTTP استفاده می‌کنید)
# VPN_API_CERT_PINS=sha256/AAAA...
