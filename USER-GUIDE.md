# راهنمای استفاده از اپلیکیشن VPN

## 🔐 مراحل اولیه (بعد از نصب)

### 1️⃣ لاگین کردن

**مهم:** بعد از نصب اپلیکیشن، باید ابتدا لاگین کنید تا بتوانید از سرورها استفاده کنید.

1. اپلیکیشن را باز کنید
2. صفحه **لاگین** نمایش داده می‌شود
3. **نام کاربری** و **رمز عبور** خود را وارد کنید
   - این اطلاعات باید از **پنل ادمین** (`http://77.110.116.139:8000/admin/auth/user/`) برای شما ساخته شده باشد
4. روی دکمه **"ورود"** کلیک کنید

**نکته:** اگر صفحه لاگین نمایش داده نمی‌شود، اپلیکیشن را ببندید و دوباره باز کنید.

### 2️⃣ مشاهده سرورها

بعد از لاگین موفق:
- به صفحه اصلی اپلیکیشن منتقل می‌شوید
- سرورهای رایگان به صورت خودکار بارگذاری می‌شوند
- می‌توانید یک سرور را انتخاب کنید و اتصال برقرار کنید

**اگر سرورها نمایش داده نمی‌شوند:**
- بررسی کنید که آیا لاگین کرده‌اید (توکن دریافت کرده‌اید)
- بررسی کنید که آیا سرورها در پنل ادمین فعال هستند (`is_active = True`)
- بررسی کنید که آیا سرورها نوع "رایگان" دارند (`server_type = "free"`)

### 3️⃣ مشاهده پروفایل و خرید اشتراک

1. روی **منوی همبرگری** (سه خط افقی) در گوشه بالا کلیک کنید
2. گزینه **"پروفایل"** را انتخاب کنید
3. در صفحه پروفایل:
   - اطلاعات حساب خود را می‌بینید
   - اگر کاربر رایگان هستید، دکمه **"ارتقا به حرفه‌ای"** را می‌بینید
   - می‌توانید سفارش خرید اشتراک را ثبت کنید

## 🔧 عیب‌یابی

### مشکل: سرورها نمایش داده نمی‌شوند

**علت‌های احتمالی:**
1. ✅ **لاگین نکرده‌اید** - ابتدا لاگین کنید
2. ✅ **توکن منقضی شده** - دوباره لاگین کنید
3. ✅ **سرورها در بک‌اند غیرفعال هستند** - در پنل ادمین بررسی کنید
4. ✅ **خطای اتصال به API** - بررسی کنید که سرور بک‌اند در دسترس است

**راه حل:**
- اپلیکیشن را ببندید و دوباره باز کنید
- از منو **"پروفایل"** → **"خروج از حساب"** → دوباره لاگین کنید
- بررسی کنید که `VPN_API_BASE_URL` در تنظیمات اپلیکیشن درست است

### مشکل: توکن‌ها خالی است

**این طبیعی است!** توکن‌ها:
- بعد از **لاگین موفق** به صورت خودکار ساخته می‌شوند
- در Django Admin در بخش **"Auth Token > Tokens"** نمایش داده می‌شوند
- اگر کاربر لاگین نکرده باشد، توکنی وجود ندارد (این طبیعی است)

**برای ساخت توکن:**
1. کاربر باید از طریق اپلیکیشن لاگین کند
2. یا در Django Admin: **"Auth Token > Tokens"** → **"ADD TOKEN +"** → کاربر را انتخاب کنید

### مشکل: گزینه پروفایل در منو نیست

**بررسی کنید:**
1. آیا لاگین کرده‌اید؟ (اگر لاگین نکرده باشید، منو ممکن است کامل نباشد)
2. آیا اپلیکیشن را به‌روزرسانی کرده‌اید؟
3. منوی همبرگری را باز کنید - باید گزینه‌های **"پروفایل"** و **"About"** را ببینید

**اگر گزینه پروفایل نیست:**
- اپلیکیشن را ببندید و دوباره باز کنید
- اگر هنوز نیست، اپلیکیشن را حذف و دوباره نصب کنید

## 📋 چک‌لیست برای ادمین

### در Django Admin (`http://77.110.116.139:8000/admin/`):

1. ✅ **کاربران** (`/admin/auth/user/`):
   - کاربر جدید ساخته شده باشد
   - نام کاربری و رمز عبور تنظیم شده باشد

2. ✅ **پروفایل کاربران** (`/admin/accounts/userprofile/`):
   - برای هر کاربر یک پروفایل وجود داشته باشد
   - نوع حساب: **"رایگان"** (برای کاربران جدید)
   - وضعیت اشتراک: **"بدون اشتراک"**

3. ✅ **سرورها** (`/admin/servers/server/`):
   - سرورها **فعال** باشند (`is_active = True`)
   - نوع سرور: **"رایگان"** یا **"حرفه‌ای"** تنظیم شده باشد
   - حداقل یک سرور رایگان وجود داشته باشد

4. ✅ **توکن‌ها** (`/admin/authtoken/tokenproxy/`):
   - بعد از لاگین کاربر، توکن به صورت خودکار ساخته می‌شود
   - اگر توکنی نیست، کاربر هنوز لاگین نکرده است

## 🎯 جریان کار کامل

```
1. کاربر اپلیکیشن را نصب می‌کند
   ↓
2. LoginActivity نمایش داده می‌شود (چون لاگین نشده)
   ↓
3. کاربر نام کاربری و رمز عبور را وارد می‌کند
   ↓
4. درخواست به /api/auth/login/ ارسال می‌شود
   ↓
5. توکن دریافت می‌شود و ذخیره می‌شود
   ↓
6. به MainActivity منتقل می‌شود
   ↓
7. درخواست به /api/servers/ با توکن ارسال می‌شود
   ↓
8. سرورها دریافت و نمایش داده می‌شوند
   ↓
9. کاربر می‌تواند سرور را انتخاب و اتصال برقرار کند
```

## ⚠️ نکات مهم

1. **همه درخواست‌های API نیاز به توکن دارند** (به جز لاگین)
2. **توکن‌ها بعد از لاگین ساخته می‌شوند** - این طبیعی است که قبل از لاگین خالی باشند
3. **کاربران جدید به صورت پیش‌فرض رایگان هستند**
4. **برای خرید اشتراک حرفه‌ای:** منو → پروفایل → ارتقا به حرفه‌ای
